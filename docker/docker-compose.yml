services:
  noanimals:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./stream_output:/app/stream_output
      - ./noanimals_settings.json:/app/noanimals_settings.json
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
